{
  "name": "interagents",
  "version": "0.1.3",
  "description": "Inter-agent messaging for AI agent sessions on the same machine.",
  "monitors": "./monitors/monitors.json",
  "userConfig": {
    "port": {
      "type": "number",
      "title": "Server port",
      "description": "Localhost WebSocket port for the interagents bus.",
      "default": 9474
    },
    "idle_shutdown_minutes": {
      "type": "number",
      "title": "Idle shutdown (min)",
      "description": "Server exits after this many minutes with no connected clients. Timer resets on every client connect/disconnect. 0 = never.",
      "default": 10
    }
  }
}
